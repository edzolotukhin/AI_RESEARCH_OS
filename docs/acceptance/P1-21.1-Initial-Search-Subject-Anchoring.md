# P1-21.1 — Initial Search Subject Anchoring

**Branch:** `acceptance/live-desk-research-01`
**Base HEAD:** `cfd7c99bb6c1c65a11613d66498aeec102eb6556`
**Mode:** offline implementation + acceptance
**Paid/live run:** none
**Commit/push:** not performed

## A. Executive verdict

**P1-21.1 PASS**

**PROPERTY X — TOPICAL SEARCH CATEGORY ANCHORING: PASS**

Initial `SearchQueryBuilder` now passes parent `ResearchQuestion.question` as `subject_context` into `build_expectation_aware_query_text`, matching targeted Search. Generic InformationNeeds can no longer emit category-free initial queries when the parent RQ carries a domain subject.

## B. Old path (fixed)

```
Planner → ResearchQuestion → InformationNeed → EvidenceExpectation
  → SearchQueryBuilder._build_query
  → build_expectation_aware_query_text(description, geography, timeframe, aspects)
  → Tavily
```

Initial Search **omitted** `subject_context`. Targeted Search already passed `question.question`.

## C. New path

```
SearchQueryBuilder._build_query
  → resolve parent ResearchQuestion
  → subject_context = question.question (or "" if empty)
  → build_expectation_aware_query_text(subject_context, description, aspects, geo, tf)
```

Same query **count** (one per IN). Semantics only.

## D. Production files

- `application/sources/search_query_builder.py`
- `application/sources/expectation_aware_query_intent.py` (docstring only)

## E. P1-20.2 12-IN replay

| Metric | Old | New |
|---|---|---|
| pizza/pizzeria category anchor | 25% (3/12) | **100% (12/12)** |
| IN2 pizza-free | yes | **no** |
| IN9–IN11 pizza-free | yes | **no** |

Artifact: `artifacts/acceptance/p1_21_1_twelve_in_query_replay.json`

## F. Tests

- `tests/application/sources/test_p1_21_1_initial_search_subject_anchoring.py` (CASE 1–25 + golden)
- Updated equality expectations in P1-07.13.1 / P1-07.17.1 initial-search regressions
- `tests/application/sources/test_search_query_builder.py` empty-query edge case

## G. Full suite

```
Ran 1783 tests in 26.615s
OK (skipped=91)
```

Untracked `scripts/p1_*.py` parked for architecture-boundary tests and restored SHA256-identically.

## H. Invariants

- No extra Search queries
- No budget/cap changes
- No LLM stage added
- No Planner / Evidence / Sufficiency / Analysis changes
- No pizza hardcoding
