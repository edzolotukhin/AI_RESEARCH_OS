# P1-07.16.1 — Bounded Remediation Attempt Evidence Envelope

**Status:** OFFLINE IMPLEMENTATION + PROOF
**Branch:** `acceptance/live-desk-research-01`
**Baseline HEAD:** `72e0f01f04b383aaadedc988eb580b3d2cd7c34a`
**Date:** 2026-08-10

No live run. No commit. No push.

---

## A. Executive verdict

**PASS**

One Profile B remediations attempt can consume at most 3 actual Evidence LLM calls. After a capped first attempt, R remaining = 3, the attempt completes normally (`bounded_partial`, history, attempt count, pending cleared), and the 15.1 scheduler can select an untouched first-opportunity gap. Local cap is not `evidence_remediation_budget_exhausted`. Global R=0 still terminates after the attempt is recorded. Default/legacy per-attempt limit remains disabled (`0`).

---

## B. Implementation boundary

| File | Role |
|------|------|
| `application/config.py` | `evidence_remediation_max_llm_calls_per_attempt` (env `EVIDENCE_REMEDIATION_MAX_LLM_CALLS_PER_ATTEMPT`, default `0`) |
| `docker-compose.full-pipeline-acceptance.yml` | Profile B overlay `= "3"` |
| `application/execution/remediation_attempt_envelope.py` | Local envelope, actual-call stop, processing states |
| `infrastructure/llm/budget_enforcing_llm_client.py` | Block generate when local envelope reached (not global exhaustion) |
| `application/evidence/evidence_extraction_service.py` | Remediation-only attempt cap; document-order first N calls |
| `application/research_quality/production_targeted_research_runner.py` | Pass config cap; persist remediations diagnostics |
| `application/research_quality/research_loop_service.py` | Complete bounded attempts; CASE 10 only for real stage death |
| `application/sources/source_need_exhaustion.py` | `bounded_partial` cannot qualify source×IN exhaustion |

Unchanged: gap scheduler 15.1, Search 13.1, extractor EE/grounding 13.2, Evidence total/R/global, targeted 2/1/1, rounds=2.

---

## C. Configuration

- Default / unset / `<=0`: legacy unlimited-per-attempt inside the remediations stage envelope.
- Positive N: at most N **actual** Evidence LLM calls per remediations attempt.
- Profile B: N=3 inside R=6. Lowcost unchanged.

`effective = min(configured, remediations_callable_capacity)` at attempt start.

---

## Q. Remaining risks

- Document-order first chunks may miss relevant late material.
- Cap=3 trades depth for breadth.
- Live research-quality effect is unproven.
- Source reuse still re-extracts from chunk 0.
- Targeted extractor still sees full EE rather than missing-aspects-only.
