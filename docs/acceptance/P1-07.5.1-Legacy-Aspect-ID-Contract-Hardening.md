# P1-07.5.1 — Legacy Aspect-ID Contract Hardening

## Status

**P1-07.5.1 LEGACY ASPECT-ID CONTRACT HARDENING — PASS (OFFLINE)**

Production boundary hardening only. Normalizer and policy semantics unchanged.

---

## A. Live failure being fixed

Run `d89ec4ff-5932-4669-b92b-722b8cb20bef` failed readiness with:

```
ValidationError: Semantic decision contains unknown aspect identifiers:
certifications, consistency, lead times, payment terms, vendor onboarding requirements
```

Root cause (P1-07.5): descriptive labels passed structured-output shape validation but failed domain normalizer on legacy path (`__legacy_need__` only).

---

## B. Canonical ID source of truth

**Module:** `application/research_quality/allowed_aspect_ids.py`

```python
resolve_allowed_aspect_ids(information_need) -> tuple[str, ...]
```

| Mode | Result |
|------|--------|
| Legacy (`evidence_expectation=None`) | `("__legacy_need__",)` |
| Expectation-backed | `EvidenceExpectation.required_aspects` |

---

## C. allowed_aspect_ids data flow

```
InformationNeed
  → resolve_allowed_aspect_ids()
  → LlmSemanticSufficiencyAssessor._system_prompt()
  → LlmSemanticSufficiencyAssessor._build_user_payload()  [allowed_aspect_ids field]
  → SufficiencyStructuredOutputGenerator.generate(allowed_aspect_ids=...)
  → RawSemanticDecisionContractGate(allowed_aspect_ids=...)
  → normalize_semantic_decision()  [unchanged final guard]
```

---

## D. First-pass prompt change

Added `render_allowed_aspect_contract(allowed_aspect_ids)` to system prompt:
- Complete allowed set for this call
- ONLY values from `allowed_aspect_ids` in output
- Never invent/decompose descriptive labels
- Legacy-specific reinforcement against semantic label substitution

---

## E. Correction prompt behavior

Correction prompt now includes the same `render_allowed_aspect_contract()` block plus schema and validation error.

---

## F. Call-scoped contract validation

`RawSemanticDecisionContractGate` extended:
1. Shape validation (unchanged)
2. **Membership validation:** `supported_aspects ∪ missing_aspects ⊆ allowed_aspect_ids`
3. Rejection code: `unknown_aspect_id`
4. Bounded diagnostics: `unknown_aspect_ids`, returned aspect lists

`semantic_conflicts` are **not** validated against aspect IDs (evidence contradiction identifiers).

---

## G. Rejection code / diagnostics

| Field | Location |
|-------|----------|
| `contract_rejection_code` | attempt telemetry |
| `allowed_aspect_ids` | attempt telemetry + `SufficiencyFailureDiagnostics` |
| `returned_supported_aspects` | attempt telemetry |
| `returned_missing_aspects` | attempt telemetry |
| `unknown_aspect_ids` | attempt telemetry + failure diagnostics |
| `information_need_id` | `SufficiencyFailureDiagnostics` |

---

## H. Retry behavior before/after

| Before | After |
|--------|-------|
| Descriptive labels pass contract → domain ValidationError | Descriptive labels → `unknown_aspect_id` contract rejection → correction retry |
| 1 sufficiency call then readiness crash | Retry within existing max attempts (3) before structured-output failure |

Retry count unchanged.

---

## I. Normalizer defense-in-depth

`normalize_semantic_decision()` unchanged. Direct invalid `RawSemanticDecision` still raises `ValidationError`.

---

## J. Legacy regression

Live five-label payload with `allowed_aspect_ids=["__legacy_need__"]`:
- Contract rejects with `unknown_aspect_id`
- All five IDs in `unknown_aspect_ids`
- Retry path can succeed with canonical ID
- No domain ValidationError on first attempt when retries remain

---

## K. Expectation-backed regression

Generic support for `allowed_aspect_ids` from `required_aspects`:
- Canonical IDs pass
- Descriptive labels rejected at contract gate

---

## L. Tests

`tests/application/research_quality/test_p1_07_5_1_legacy_aspect_id_contract_hardening.py` — 18 tests

Matrix A–O covered.

---

## M. Limitations

- Dynamic provider JSON Schema enum not implemented (static schema + contract gate used)
- Readiness task snapshot still depends on `SemanticSufficiencyAssessmentError.diagnostics` for failure visibility
- Planner still does not emit `EvidenceExpectation` in production

---

## N. Recommendation for next controlled E2E

**GO** — one controlled diagnostic E2E to verify:
1. Unknown descriptive labels trigger contract retry (not domain ValidationError)
2. Corrected canonical legacy response completes readiness
3. Failure diagnostics expose `unknown_aspect_ids` if retries exhaust

Do **not** mark P1-07 E2E accepted yet.

---

## Confirmations

- No fuzzy matching
- No descriptive-label mapping
- Normalizer not weakened
- No provider calls / no live E2E in this milestone
- No commit / no push
