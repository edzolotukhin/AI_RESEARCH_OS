# P1-27.1 — Sufficiency Reason-Bound Alignment

## Verdict

PROPERTY AE — SUFFICIENCY REASON-BOUND ALIGNMENT: **PASS**.

The P1-27 forensic root cause was a prompt-contract mismatch: the active RawSemanticDecision validator rejected `reason` values longer than 500 Python characters, while the first model request described the field only as concise and the illustrative schema described it only as a string. A retry disclosed neither the failing field nor its numeric bound.

## Canonical bound

The active semantic-sufficiency path uses `DEFAULT_RAW_SEMANTIC_MAX_REASON_CHARS = 500` in `application/research_quality/raw_semantic_decision_contract.py`. The local validator continues to be authoritative:

- nonblank reason of 499 characters: accepted;
- nonblank reason of 500 characters: accepted;
- reason of 501 characters: rejected as `reason_too_long`;
- length remains Python `str` character length, not encoded byte length.

The former semantic-sufficiency payload module also contains a legacy 500-character constant, but production `LlmSemanticSufficiencyAssessor` explicitly supplies the RawSemanticDecision schema/validator. PROPERTY AE derives both new model-facing messages from the active RawSemanticDecision constant and removes the duplicated 500 literal from its existing unit test.

## First-attempt alignment

The shared RawSemanticDecision output instructions now state unambiguously that `reason` must be a concise, non-empty explanation of at most 500 characters. `LlmSemanticSufficiencyAssessor._system_prompt()` already consumes this shared renderer for every semantic Sufficiency call, so the numeric contract is visible before the first generation and is provider/model/domain independent.

## Provider/schema visibility

The current LLM abstraction does not support a native response JSON Schema. `LLMGenerationOptions` carries only output-token and reasoning-effort options. The displayed payload “schema” is an illustrative JSON object in prompt text and does not support `maxLength` enforcement.

Accordingly, this milestone does not fake native schema enforcement or broaden the provider abstraction. The numeric bound is provider-visible through the prompt; the local validator remains final authority.

## Corrective retry

When `RawSemanticDecisionContractGate` reports `reason_too_long`, the next bounded correction prompt now states:

- failing field: `reason`;
- problem: value is too long;
- maximum: 500 characters, derived from the canonical constant;
- required action: return corrected JSON with a non-empty reason fitting the maximum.

The correction retains the existing bounded response preview and ordinary contract context. It exposes no stack trace, credential, or provider internal. Other structured-output errors retain their existing generic correction behavior; truncated output retains its existing compact regeneration instruction.

## Preserved semantics

- maximum attempts remains 3;
- valid JSON with an overlong reason remains a contract-validation failure, not malformed JSON;
- no truncation, summarization, fabricated reason, or automatic semantic repair was introduced;
- exhausting all attempts still raises the existing structured/semantic technical error and fails closed;
- no Sufficiency criteria, status aggregation, thresholds, Evidence, Search, retrieval portfolio, submission behavior, provider, model, budget, persistence, schema, dependency, UI, Analysis, Report, or Review behavior changed.

## Deterministic failure replay

Offline fake-client coverage reproduces the P1-26.2 failure class:

1. valid JSON with reason length 501 is rejected;
2. retry prompt explicitly communicates field and maximum;
3. a second overlong reason is rejected and receives the same corrective contract;
4. a final 500-character reason is accepted;
5. three overlong responses exhaust exactly three attempts and preserve fail-closed behavior.

## Acceptance matrix

| Case | Result |
|---|---|
| First prompt contains canonical maximum | PASS |
| Prompt maximum equals validator constant | PASS |
| reason 499 / 500 / 501 | PASS / PASS / `reason_too_long` |
| Empty reason / one character | rejected / accepted |
| Unicode character semantics | PASS; Python character count preserved |
| Valid JSON + overlong reason | contract-stage rejection |
| Corrective feedback identifies `reason` | PASS |
| Corrective feedback includes 500 maximum | PASS |
| Corrective feedback instructs correction | PASS |
| Retry count unchanged | PASS; 3 |
| One invalid then valid | PASS |
| Two invalid then final valid | PASS |
| All attempts invalid | fail-closed preserved |
| Silent truncation / fabricated reason | absent / absent |
| Provider-visible prompt contract | PASS; native schema unavailable |
| Ukraine pizza fixture | PASS |
| New Zealand residential heat-pump fixture | PASS |
| India UPI merchant-payment fixture | PASS |
| German industrial-electricity fixture | PASS |
| P1-26.2 IN4 failure-class replay | PASS |

## Offline verification

- PROPERTY AE focused: **18/18 PASS**.
- Focused plus existing contract/prompt/retry suites: **82/82 PASS**.
- Application research-quality suite: **414/414 PASS**.
- Infrastructure research-quality suite: **24/24 PASS**.
- P1-26.2D/P1-26.2B/P1-26.1/P1-25.1/P1-24.1/P1-23.1/P1-22.1/P1-21.1: **166/166 PASS**.
- P1-17.1/P1-14/P1-09/P1-08: **75/75 PASS**.
- P1-19/P1-20: **181/181 PASS**.
- Full suite: **1943 tests, 0 failures, 0 errors, 91 skipped**.

The first full-suite pass was contaminated by existing untracked forensic Python helpers. A narrow verified rerun parked 89 untracked Python helpers under `scripts/` and `artifacts/` by reversible extension rename. Every helper was restored to its exact path; SHA-256/size restore mismatches: **0**.

## External activity and boundaries

Provider calls: NONE. LLM calls: NONE. Paid/live Research: NONE. New Research: NONE. Existing-run mutation: NONE. Persistence/schema/dependencies: unchanged. PROPERTY AC, PROPERTY AD, and PROPERTY AD-P: unchanged.
