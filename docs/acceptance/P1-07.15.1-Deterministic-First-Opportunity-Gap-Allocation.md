# P1-07.15.1 — Deterministic First-Opportunity Remediation Gap Allocation

**Status:** OFFLINE IMPLEMENTATION + PROOF
**Branch:** `acceptance/live-desk-research-01`
**Baseline HEAD:** `b111d27c6affcb021cd483e767b59ef187562b86`
**Date:** 2026-08-10

No live run. No commit. No push.

---

## A. Executive verdict

**PASS**

First-opportunity cohort outranks repeat attempts. Improved IN1 no longer takes immediate attempt 2 while other blocking gaps have 0 attempts (`a04e8b35` regression). Non-improved same-round stall unchanged (`4fac4000` / CASE 3/9). Attempts/gap=2, rounds=2, R=6, extractor, Search, Sufficiency, 14.1 exhaustion unchanged. Scheduler adds 0 LLM/Search/Evidence calls.

---

## B. Exact old scheduler selection semantics

Eligible = actionable status ∩ attempts &lt; max ∩ not round-stalled.

Walk `select_actionable_gaps` order `(rq_id, information_need_id)` and return the first eligible.

Status filtered membership only. An improved gap stayed first in that walk → immediate attempt 2.

---

## C. Implementation

| File | Change |
|------|--------|
| `application/research_quality/gap_scheduler.py` | `decide_next_actionable_gap` + `GapSchedulerDecision`; `select_next_actionable_gap` wraps it |
| `application/research_quality/research_loop_service.py` | Record decision after remediations/Sufficiency gates, before runner |
| `application/research_quality/research_loop_state.py` | `scheduler_decisions` list on existing shared loop state |
| `tests/.../test_p1_07_15_1_first_opportunity_gap_allocation.py` | CASE 1–14 + historical sims + loop proofs |
| `tests/.../test_targeted_research_loop.py` | `test_max_rounds_*` expects IN1 then IN2, not IN1×2 |

**Cohorts:** `first_opportunity` (`attempt_count==0`) then `repeat_opportunity` (`attempt_count>0` and still eligible).
**Tie-break:** `(research_question_id, information_need_id)`.
**Observability:** eligible IDs, attempt counts, cohorts, selected, reason, tie-break, remaining remediations Evidence calls, stalled, prior improved. Appended only when an attempt is allowed to start (no fake IN2 if R=0).
**Stall / improved / attempt limit:** unchanged; they still define eligibility. Policy only reorders among eligible.

Round-stall does **not** block first-opportunity across rounds (`attempt_count` persists; cohort 0 still wins). Round semantics not changed.

---

## D. Before / after

| Scenario | OLD | NEW |
|----------|-----|-----|
| a04e8b35 IN1 attempt 1 improved; IN2/IN3 untouched | IN1 again | **IN2** |
| 4fac4000 IN1 non-improved/stalled | IN2 | **IN2** (no regression) |
| All first opportunities used; repeats eligible | `(rq,in)` among them | same repeat cohort, `(rq,in)` → IN1 |

---

## E. Budget limitation proof

Scheduler still only checks **≥1 remediations Evidence call** before starting an attempt. Extract chunking (8000 chars) can consume all **R=6** inside that attempt. First-opportunity is **ordering fairness**, not a guarantee of multiple completed gaps. If control never returns, IN2 is not recorded as a completed opportunity (CASE 10).

---

## F. Historical simulation

| Run | After attempt 1 | Next under 15.1 |
|-----|-----------------|-----------------|
| a04e8b35 improved IN1 | IN2 | proven offline |
| 2b91089c / d4db3024 non-improved | IN2 if control returns | proven; live R=6 may still die mid-IN1 |
| 4fac4000 stalled IN1 | IN2 | proven; live IN2 start may still be incomplete |

No invented Evidence outcomes.

---

## G. Tests

- Focused 15.1: **OK**
- Targeted loop + P1-07.10: **OK**
- 14.1, 13.2, 13.1, 12.1, 12, 11 incremental + Profile B: **OK**
- research_quality application **339 OK** + domain **138 OK**
- Execution budget: 2 pre-existing isolation ERRORs (`downstream_reserve_exhausted` vs default envelope) — unrelated to scheduler
- `python run_tests.py`: **1412** ran; **1 FAIL** = untracked forensic `json.loads` scripts (parked → architecture test OK → restored byte-identical); **skipped=91**

---

## H. Invariant proof

1. First-opportunity outranks repeats — **yes**
2. Improved gap may get attempt 2 later — **yes** (repeat cohort)
3. Non-improved stall unchanged — **yes**
4. Status not priority — **yes**
5. Attempts/gap = 2 — **yes**
6. Rounds = 2 — **yes**
7. R=6 unchanged — **yes**
8. No cost prediction/reservation — **yes**
9. No extra LLM/Search/Evidence calls — **yes**
10. Source exhaustion separate — **yes**
11. Search/extractor/Sufficiency/readiness unchanged — **yes**
12. Fail-closed termination unchanged — **yes** (CASE 10 remediations stop)

---

## I. `git diff --check`

Clean (CRLF warnings only; no whitespace errors).

---

## J. `git status --short`

Tracked mods: `gap_scheduler.py`, `research_loop_service.py`, `research_loop_state.py`, `test_targeted_research_loop.py`.
Untracked: `tests/.../test_p1_07_15_1_first_opportunity_gap_allocation.py`, this doc, prior forensic leftovers.

HEAD still `b111d27` + local uncommitted 15.1 work.

---

## K. Remaining risks

- Chunked remediations extract can still spend all R=6 on one attempt.
- First-opportunity ≠ guaranteed budget fairness.
- Live product effect **unproven** (no Profile B run this phase).

---

## L. Commit recommendation

Implementation is offline-complete and ready to commit when asked.

**Do NOT commit. Do NOT push. Do NOT run Profile B/C.**
