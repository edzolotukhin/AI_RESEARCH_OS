# Copy to .env and fill in values before running main.py or other live LLM paths.
# Tests do not require OPENAI_API_KEY.

# Required for live OpenAI calls (read by the OpenAI SDK after python-dotenv loads .env)
OPENAI_API_KEY=
